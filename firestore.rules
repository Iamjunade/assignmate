rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all access for debugging
    // WARNING: Revert this to secure rules before production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
